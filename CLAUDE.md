# Project Instructions

Follow the repository working agreement in [`AGENTS.md`](AGENTS.md). In
particular, documentation updates are part of every change and must not be
deferred until the project owner asks. Completed, verified milestones must also
be committed and pushed to the configured GitHub `origin` as the project's
cloud backup; follow the scoped-commit rules in `AGENTS.md`.
