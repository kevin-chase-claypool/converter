# Lab Note: YYYY-MM-DD - Test title

## Objective

State the question this test answers.

## Configuration

- Hardware revisions:
- Wiring/pin map:
- Firmware commit/build:
- grblHAL settings:
- Converter settings/sample:
- Instruments:

## Code, commands, and configuration used

If firmware, a test sketch, a shell command, a G-code command, or a controller
configuration is involved, paste the exact version used below in a fenced code
block. Also identify the source file and commit/build when available. Do not
substitute a filename or prose summary for the runnable/testable content.

```text
Paste the exact code, command sequence, or configuration used for this test.
```

## Procedure

1. 

## Results

Record measurements, pass/fail, photos, plots, logs, and unexpected behavior.

## Difficulties and corrective actions

Record every meaningful difficulty, failed attempt, incorrect assumption,
intermittent condition, or unexpected result. For each, state:

- What was observed and how it was diagnosed.
- What changed to correct it (wiring, soldering, configuration, firmware,
  replacement part, or test setup).
- The repeat result that demonstrated the correction worked.

Write `None encountered` only if the test genuinely had no difficulty or retry.

## Interpretation

Explain what the result means for the system design.

## Decisions and next action

Link any new ADR or changed interface, then name the next concrete test.
